<p id="notice"><%= notice %></p>

<h1>Sessaos</h1>

<table>
  <thead>
    <tr>
      <th>Mentor</th>
      <th>Mentorado</th>
      <th>Horario</th>
      <th>Habilidade</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sessaos.each do |sessao| %>
      <tr>
        <td><%= sessao.mentor_id %></td>
        <td><%= sessao.mentorado_id %></td>
        <td><%= sessao.horario %></td>
        <td><%= sessao.habilidade %></td>
        <td><%= link_to 'Show', sessao %></td>
        <td><%= link_to 'Edit', edit_sessao_path(sessao) %></td>
        <td><%= link_to 'Destroy', sessao, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Sessao', new_sessao_path %>
